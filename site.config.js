/* MCCPS site config — edit and redeploy.
   1) Create a free form at https://formspree.io (email-notified submissions).
   2) Paste the form ID (the part after /f/) below. All three forms (quick contact,
      free analysis, agent application) post to it; each sends a distinct _subject.
   Note: file uploads on the Free Analysis form require a Formspree plan that
   supports attachments; on the free plan the text fields still arrive. */
window.MCCPS_CONFIG = {
  formspreeId: 'YOUR_FORMSPREE_ID'
};
